# AI 协同审核 GUI 自动弹出 —— 落地清单

> 前置文档：《AI协同审核GUI自动弹出技术方案.md》（已含 §9 审查修订记录）。
> 本文是**逐条对照源码核查后**给出的结论与可执行清单。所有改动点均标注**文件与行号锚点**，验收项均可证伪。

---

## 1. 审查结论：可以写成落地清单，但必须先补 3 个硬阻塞

方案**方向成立**——"一个协调器 · 一份真相 · 一把锁"与 req-guard 现有架构（core 唯一真相、L1/L2/L3、审批锁）完全同构，且结果回传的"文件兜底通道"与现有 `gate_check` 天然兼容、零改动。

但核查发现 **3 个会导致清单建在空地基上的硬阻塞**，必须先解：

| # | 阻塞 | 为什么致命 | 证据 |
| --- | --- | --- | --- |
| **B1** | 拦截原因**无结构化字段** | 方案整套触发逻辑依赖"拦截原因分类 + `req_id`/`step` 去重键"，而今天 `GateVerdict::Block` 只有脚本 stderr 的**中文原文**。且项目铁律禁止解析人类可读文案——这是"看着在拦、其实没拦"的同源坑 | `gate.rs:495,606`；脚本 `gate.rs:1327-1363` |
| **B2** | 零依赖下**没有跨平台 IPC** | 原方案写"Unix socket / Windows 命名管道"：前者非 Windows 原生，后者不在 `std`，而 `core` 零依赖是立身之本 | `core` 无 `[dependencies]` |
| **B3** | **AI 启动的 GUI 会继承 `REQ_GUARD_AI_CTX` → 人类批准被"锁"拒绝 → 功能自废** | AI 会话被注入该变量，子进程继承；GUI 里点批准会走 `ensure_human` 被拒。方案原本完全没意识到这个交互 | `requirement.rs:197` → `auth.rs:59`；注入点 `gate.rs:831` |

**决定**：写，但按"先补地基、再建能力"分 P0–P5 落地；**P0（结构化拦截原因）是所有后续的前提**，B2/B3 在各自阶段内解决。

### 强烈建议的范围裁剪（先做 MVP）

完整版要做的**双向 IPC**（raise 已运行实例 + 结果回推）复杂度最高。建议首期只做：

> **MVP = P0（结构化）+ 单实例互斥 + 请求桥 + GUI 启动即聚焦**，
> **结果回传全部走"文件兜底通道"**（AI 自跑 `gate_check` 感知）——这一环**本来就是零新增且最稳的**。

这样可砍掉 P2 的协议设计与 P4 的后台监听线程，首期工作量减半，且行为上"多 AI 不重复弹窗 + 人类批准后 AI 自然解封"已经成立。完整版（已运行实例聚焦 + 结果推送）列为 P4+。

---

## 2. 落地清单

> 通用约束：**`core` 永远零依赖**（不得加 `[dependencies]`，P2 只能用 `std`）；
> 脚本改动**必须 sh / ps1 双端同步**（BOM、fail-closed 语义一致）；
> 每阶段出口需 `cargo test --workspace` + `clippy --workspace --all-targets -- -D warnings` + `fmt --check` 全绿。

### P0 — 结构化拦截原因（硬前置，无此项后面全部落不了）

| 项 | 内容 | 锚点 |
| --- | --- | --- |
| 目标 | 拦截结论变成**机器可读**，AI 能拿到"是否需人工决策 / 哪个需求 / 哪一段" | — |
| 改动 1 | 拦截脚本在各拦截分支输出机器可读标记：`REQ_GUARD_BLOCK_KIND=<no_requirement\|need_approval\|need_resolve\|not_installed>`、`REQ_GUARD_REQ=<需求ID>`、`REQ_GUARD_STEP=<首个未通过段>` | `gate.rs:1327-1363`（sh）、`HOOK_PS1`（ps1，双端同步） |
| 改动 2 | `GateVerdict::Block` 增加结构化字段（`kind` / `req` / `step`），由 `gate_check` 解析标记得到；**禁止用中文文案匹配** | `gate.rs:482,602` |
| 改动 3 | **需求 ID 口径**：脚本里的"活跃需求"是**文件名** `$ACTIVE`（如 `REQ-001-xxx.md`），与 `GATE:HEAD id=` 的需求 ID 不是一回事。标记必须输出**需求 ID** | `gate.rs:1316-1321` |
| 验收（可证伪） | ①`req-guard check` 在四种拦截场景下给出各自 `kind`；②故意改写中文提示文案，**解析结果不变**（证明没依赖文案）；③`REQ-001-xxx.md` 场景输出 `REQ_GUARD_REQ=REQ-001` 而非文件名；④ps1 在 PowerShell 5.1 下同样输出（BOM 回归） | — |
| 风险 | 脚本改坏 = 全局拦截失效（fail-closed 会**全拦**）。必须跑 `scripts/verify_gate.py` 全场景 | — |

### P1 — 环境事实：`ci` 判定（解 D2）

| 项 | 内容 | 锚点 |
| --- | --- | --- |
| 改动 | `EnvFacts` 增加 `ci`（`CI` / `GITHUB_ACTIONS` / CNB 等），并新增可单测的"是否允许自动弹窗"判定函数 | `ui_mode.rs:61-72` |
| 验收 | 决策表单测覆盖 `ci × ssh × display × windows/macos` 组合矩阵；CI 下判定为"不弹窗" | — |

### P2 — 单实例锁 + IPC（解 B2 / D3，核心新增）

| 项 | 内容 | 锚点 |
| --- | --- | --- |
| 目标 | 全局只有一个 GUI；后来的 AI 转为 client | — |
| 改动 1 | 新增 `core::instance`（零依赖）：锁文件路径复用 `token::guard_dir()` 既有约定 | `token.rs:25,40` |
| 改动 2 | **原子抢锁**：`OpenOptions::new().create_new(true)`；锁文件内容 = `pid` + `port` + 心跳时间戳 | — |
| 改动 3 | **传输必须是回环 TCP**（`127.0.0.1`，`std::net`）——零依赖下唯一跨平台 IPC；`bind("127.0.0.1:0")` 取动态端口写入锁文件，**不固定端口** | 方案 §2.3 已修订 |
| 改动 4 | **僵尸锁回收用心跳**（GUI 每帧/定时刷新时间戳，超时视为死进程），避免依赖子进程查 pid | — |
| 改动 5 | 协议用单行 JSON，**复用现有零依赖解析器 `core::json`** | `core/src/json.rs` |
| 验收 | ①两进程并发抢锁 → 恰好一个成功；②心跳过期后锁可被回收；③协议往返解析单测；④`core/Cargo.toml` **仍无 `[dependencies]`** | — |
| 风险 | 回环端口被安全软件拦截（罕见）；抢锁与心跳的竞态需单测覆盖 | — |

### P3 — 请求桥 CLI（解 D1 / B3）

| 项 | 内容 | 锚点 |
| --- | --- | --- |
| 改动 1 | `Action::Ui` 扩展 `--request <需求ID>` / `--step` / `--requester` / `--probe` | `cli.rs:43-44,78-81` |
| 改动 2 | `run_ui` 分流：probe → 打印结构化探测结果退出；request → 先过准入（P0 的 `kind` + P1 的 `ci` + display），再抢锁或转 client | `main.rs:348-408` |
| 改动 3 | **★ 启动 GUI 前显式清除 `REQ_GUARD_AI_CTX`**（解 B3），否则人类在 GUI 里点批准会被"锁"拒绝 | `auth.rs:25,59` |
| 改动 4 | 降级：无 display → TUI 或打印人工指引；CI → 打印结构化 block 并 `exit 1`（AI 停下等人） | — |
| 验收 | ①AI 会话内（带 `REQ_GUARD_AI_CTX=1`）经请求桥打开 GUI → 人类批准**通过**；②CI 下不弹窗且退出码非零；③`--probe` 输出可被 `core::json` 解析 | — |
| **安全代价（须写明）** | 清除 `AI_CTX` 后，方案 A 对"GUI 启动路径"失去区分力 → **生产环境必须启用方案 B（令牌）或方案 C**，不能只靠方案 A | 规范 §4.4 |

### P4 — GUI 侧接管（解 D4，MVP 可只做前半）

| 项 | 内容 | 锚点 |
| --- | --- | --- |
| MVP | `run()` 接受"聚焦目标"（需求 ID + 段），启动后直接定位到该需求该段 | `gui/lib.rs:22-39` |
| 完整版 | 后台线程 `accept` + `mpsc` 推给 App；App 维护**审核请求注册表**（key = 需求 ID + 段，去重），渲染"待审核请求"区 | `gui/src/app.rs` |
| 完整版 | 人类决策落盘后 → 重跑 `gate_check` → 向注册的 requester 回推结果；断连忽略（文件兜底） | — |
| 验收 | ①两个 AI 先后请求同一需求 → **只一个窗口**；②人类批准后两 AI 自跑 `check` 均通过；③GUI 真机点击验证（沿用 2026-09-17 的 SendInput 坐标校准经验） | — |

### P5 — 收口

| 项 | 内容 |
| --- | --- |
| 文档 | 更新《技术方案.md》《AI工具合规保证规范.md》（新增能力、B3 的安全代价、GUI 审批仍受"锁"约束） |
| 校验 | 锁文件是**运行时产物**，不进 `install --verify`；如未来要校验，须确认其平台中立性（沿用 `marker_of` 词干原则） |
| CI | 与既有 P5（三平台 GUI 产物矩阵）合并推进 |

---

## 3. 不建议本期做

| 项 | 理由 |
| --- | --- |
| TUI 侧对称实现单实例/注册表 | 首期把"无 display"降级为 CLI 人工指引即可；TUI 是 SSH 场景，AI 与人不共屏，价值低 |
| 结果回推的可靠投递（重连、持久化队列） | 文件兜底通道已够用；做可靠投递会把 P2 变成消息队列项目 |
| 让 AI 驱动 GUI 操作 | 与"审批锁"直接冲突，禁止 |

---

## 4. 一句话总结

方案**可以落地**，但顺序不能颠倒：**先 P0 把拦截原因结构化**（否则整套触发逻辑没有输入），再建单实例与请求桥（注意 IPC 只能是回环 TCP），最后接 GUI（记得清 `REQ_GUARD_AI_CTX`，否则功能自废）。首期建议只做 MVP，把结果回传交给已有的文件兜底通道。
