# 技术方案：req-guard（AI 需求门禁，Rust 零依赖）

> 配套《需求分析报告.md》。工程要点：**零依赖 Rust crate + 脚本判定 + 双端等价**。

---

## 1. 架构

### 1.1 模块划分

| 模块 | 职责 | 关键符号 |
| --- | --- | --- |
| `cli` | 手写参数解析（12 个命令） | `parse()`, `Action`, `Args` |
| `requirement` | 清单生命周期：创建/审核/状态；GATE 标记解析 | `STEPS`, `create()`, `review()`, `token()` |
| `comment` | 审核评论：添加/回复/关闭/锚点重算 | `Comment`, `add()`, `resolve()`, `refresh_anchors()` |
| `gate` | 拦截脚本、AI 工具 hook 注入、pre-commit、绕过 | `install()`, `check()`, `bypass()`, `HOOK_SH/PS1` |
| `error` | 统一错误 | `GateError`, `Result<T>` |

依赖单向：`cli → {requirement, comment, gate} → error`，无循环。

### 1.2 判定逻辑只有一份

拦截判定在 `.gates/hooks/req-guard-check.{sh,ps1}`（脚本），
`req-guard check` 直接调用脚本并透传退出码——保证"本地验证的即线上拦截的"，消除逻辑漂移。

---

## 2. 数据模型

### 2.1 清单（`REQ-00N-*.md`，人机双读）

```markdown
<!-- GATE:HEAD id=REQ-001 status=in_review created=2026-09-09 -->
<!-- GATE:STEP name=decomposition label=需求分解 status=approved reviewer=寇工 updated=2026-09-09_2210 -->
```

- 正文给人看；`GATE` 标记行**只能由命令改写**，防 AI 自提 approved；
- 字段值禁止空白（`safe_field` 把空格转 `_`），否则行解析断裂；
- 状态机：步骤级 `pending→approved|rejected`；清单级三步全 approved 才解锁，
  任一 rejected → `changes_requested`。

### 2.2 评论（`REQ-00N-*.comments.md`，**独立文件**）

```markdown
<!-- GATE:COMMENT id=C001 step=solution author=寇工 ts=... state=open blocking=true line=42 reply=- stale=false -->
> quote: 回滚方案需人工介入
> ctx_before: - [ ] 风险点与回滚方案
> ctx_after:  - [ ] 影响面评估

数据库迁移的回退步骤需要明确。
<!-- /GATE:COMMENT -->
```

- `reply=C00N` 记录**回复关系**（AI 只能以回复形式参与讨论，见 §2.3）；
- `state=open|resolved` 决定是否影响门禁；`blocking=true` 且未 resolve 即拦截。

**为什么独立文件**（修正内嵌方案）：清单正文对 AI 可写，内嵌评论则 AI 改正文时可顺手删改
审核意见——"嫌疑人修改证据"。独立后配合三道保护：

| 道 | 机制 |
| --- | --- |
| 1 | L1 hook 第 0 步：从 PreToolUse 的 **stdin JSON** 提取 `file_path`，匹配 `*.comments.md` 即拒绝 |
| 2 | 命令层：`--author ai` **必须带 `--reply`**（否则拒绝），且 `--reply` 必须指向已存在的评论；resolve 拒绝 `author=ai` |
| 3 | hook 只作用于 AI 工具，审核人用 CLI/编辑器不受限 |

**行号锚定与漂移**：`line` 为记录时行号；重定位优先按 `quote`(+`ctx`) 模糊匹配回写，
未命中标记 `stale=true` 并降级为步骤级展示；`--refresh-anchors` 手动重算。

---

## 3. 拦截脚本设计（唯一判定逻辑）

**执行顺序**（顺序即安全）：

| 步 | 检查 | 失败行为 |
| --- | --- | --- |
| 0 | AI 写 `*.comments.md`（stdin JSON） | **拦截**——★ 必须先于绕过：逃逸阀不覆盖证据完整性 |
| 1 | 应急绕过令牌有效期内 | 放行 + 审计 |
| 2 | 存在活跃需求（跳过 `done`，**排除 `*.comments.md`**） | 拦截：提示 `create` |
| 3 | 三段全 `approved` | 拦截：列出未完成步骤 |
| 4 | 无 `open+blocking` 评论 | 拦截：需审核人 resolve |
| 5 | 评论摘要输出（非阻塞提示） | — |

> 第 2 步必须排除评论文件：`REQ-002.comments.md` 在字典序降序里可能排在
> `REQ-001.md` 之前，一旦被当成"活跃需求"，其正文没有 `GATE:STEP` → 三步全 pending
> → **恒拦截**（且原因难以察觉）。

> **fail-closed 原则**：req-guard 是安全机制，脚本缺失时 pre-commit 片段**拦截**并提示初始化
> （区别于 sql-guard/java-guard 的二进制缺失 fail-open——那是质量检查，可跳过）。

脚本仅用 `grep/sed/date`，stdin 检测用 `[ ! -t 0 ]`（避免 pre-commit 场景 `cat` 挂起）。

---

## 4. AI 工具 hook 注入

| 工具 | 配置路径 |
| --- | --- |
| `claude` | `.claude/settings.json` |
| `codex` | `.codex/hooks.json` |
| `codebuddy` | `.codebuddy/hooks.json` |
| `cursor` | `.cursor/hooks.json` |

注入内容：`PreToolUse` 拦截 `Write|Edit|MultiEdit|NotebookEdit`。
幂等与不破坏：已存在且含 `req-guard-check` → 跳过；已有但不含 → **不覆盖**，提示手工合并。

pre-commit：**追加**到 gates-toolkit 既有 hook 之后（不覆盖）。

---

## 5. 错误处理与退出码

```rust
pub enum GateError { Io{path,source}, Validation(String), External{command,status,stderr} }
```

| 退出码 | 含义 |
| --- | --- |
| 0 | 成功 / 放行 |
| 1 | 运行期错误 / **拦截**（CI 据此阻断） |
| 2 | 参数/校验错误 |

---

## 6. 验证状态（透明说明）

> 更新于 2026-09-11（P0/P1 修复后，真机复验）。

| 项 | 命令 | 结果 |
| --- | --- | --- |
| 编译 | `cargo check --all-targets` | ✅ 零错误零警告 |
| 静态检查 | `cargo clippy --all-targets -- -D warnings` | ✅ 零警告 |
| 格式 | `cargo fmt --all` | ✅ |
| 单元测试 | `cargo test` | ✅ 26 passed |
| 拦截脚本场景 | `python scripts/verify_gate.py` | ✅ 10/10 通过 |

**端到端真机验证**（Windows 11 + Git Bash + MSVC 2022，`init` 起全流程）：

- 命名一致：`init` 产出 `.gates/req-guard.yaml` 与 `.gates/hooks/req-guard-check.{sh,ps1}`，
  与 gates-toolkit 片段 `030-reqguard` 引用的路径一致（回归测试锁定）；
- ps1 带 UTF-8 BOM → PowerShell 5.1 正常执行，`req-guard check` 不再"假拦截"；
- 跳步审核被拒；`.gates/req-guard.yaml` 置 `strict_order: false` 后可跳步（配置真实生效）；
- 阻塞评论 → **L2 `git pre-commit` 真机拦下提交**（`exit=1`，仓库无提交产生）；
- AI 新开评论被拒、AI `resolve` 被拒、AI `reply` 落盘 `reply=C00N`；
- 评论新增/关闭、绕过、放行、拦截事件全部写入 `.gates/audit/gate-audit.log`。

**本机构建前提**（Windows + Git Bash）：`/usr/bin/link`（GNU coreutils）会遮蔽 MSVC `link.exe`，
需将 MSVC `bin/Hostx64/x64` 前置到 `PATH`，并设置 `LIB`（MSVC `lib/x64` + Windows Kits `um/x64`、`ucrt/x64`）。

**待补**：CI 多平台矩阵（P5）、UI（P0–P3）尚未启动。

---

## 9. UI 方案（egui + TUI 双模）

门禁管理台的图形界面**归属本工具**（dev-scaffold `架构决策记录.md` ADR-002：
UI 管的是清单与审核，随门禁走）。

| 文档 | 内容 |
| --- | --- |
| 《UI架构细化方案.md》 | workspace 拆分、core API、GUI/TUI 详细设计、落地阶段 P0–P5 |
| 《GUI跨平台方案选型.md》 | egui 可行性评估、S0–S5 方案对比、跨平台构建矩阵 |

要点：

- **workspace**：`core`（零依赖 lib）/ `cli`（唯一 bin，feature 门控）/ `tui`（ratatui 0.30）/ `gui`（eframe 0.36.1）；
  `tui`/`gui` 是 **lib**，可被链进同一二进制实现"单文件 + 自动选界面"；
- **feature**：`default`（纯 CLI，零依赖）/ `tui` / `gui` / `full`（三合一）；
- **命令**：`req-guard ui`（无参自动探测：桌面→egui、纯命令行/SHH→TUI，`--gui`/`--tui` 强制）；
- **已定决策**：正文**只读**、中文字体 **Noto Sans SC 子集**、分发 **cli + full 双产物**、不保留独立二进制；
- **落地顺序**：P0 workspace 化 → P1 core API 结构化 → **P2 TUI 先行** → P3 GUI → P4 合一与回退 → P5 CI 矩阵。
