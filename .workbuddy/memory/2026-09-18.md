# 2026-09-18 req-guard 工作日志

## 文档梳理：全部文档归拢到 docs/（按主题分类 + 逐篇审阅）

### 最终结构
```
docs/README.md              索引（导航 / 状态 / 清理记录 / 维护约定）
docs/需求/需求分析报告.md
docs/设计/技术方案.md、UI架构细化方案.md、GUI跨平台方案选型.md
docs/规范/AI工具合规保证规范.md
docs/提案/AI协同审核GUI自动弹出技术方案.md（未实现；§10 = 落地路径 P0–P5）
```
根目录只留 `README.md`（项目入口）；`.workbuddy/memory/` 属 AI 工作记忆，**不**纳入 docs（移动会破坏记忆系统）。

### 逐篇审阅结论
- 保留 6 篇；4 篇加「状态校注（2026-09-18 复核）」块（置于文首，列出过时点 + 以哪份为准）：
  需求分析报告（grep/sed 判定已被 hook-check 真解析取代、命令/目录清单缺 6 项）、
  技术方案（模块图缺 gui/auth/token/digest/json、§6 验证数字是 09-12 快照）、
  UI架构细化方案（P0–P4 已完成 P5 仍部分、§9 待确认已决策、zigbuild 表述过时）、
  GUI跨平台方案选型（gui 最终是 lib 非 bin、core 模块清单含 dev-scaffold 遗产、构建矩阵已被取代）。
- **删除 1 篇**：《AI协同审核GUI落地清单.md》——一次性源码核查快照，全部证据是"文件:行号"锚点
  （必然腐坏 = 错误信息源），且与方案 §9 重复。其可执行内容已并入方案文档新增的 §10「落地路径」，
  锚点改写为"文件名 + 符号名"。恢复：`git checkout HEAD~1 -- AI协同审核GUI落地清单.md`。
- README 修正：文档引用改为 docs 相对路径、新增「文档」导航表、去掉写死的 63 用例
  （改为以实跑为准；`#[test]` 实际 82 个）、场景数改为「13 固定 + 1 条件」。

### 维护约定（写进 docs/README.md）
新文档只进 docs/ 对应分类；引用代码位置**写文件名+符号名，不写行号**；
验证数字只记快照日期；改门禁保证能力须同步规范第 2.2/4/6 章。

### 本机踩坑（与构建相关，非文档）
- Git Bash 里 `cargo` 不在 PATH，实际在 `~/.cargo/bin/cargo.exe`。
- 即便显式设 `CARGO_TARGET_X86_64_PC_WINDOWS_MSVC_LINKER` + `LIB` 指向 MSVC 14.44.35207 /
  Win Kits 10.0.22621.0，`cargo test --workspace` 仍在链接期失败（先 LNK 1104，后 fingerprint 写入失败），
  **未能拿到真实用例数** → 文档里不再写死数字。`/usr/bin/link`（GNU coreutils）仍在 PATH 里遮蔽 MSVC link.exe
  （`which -a link` 只有 /usr/bin/link），要用 MSVC 需前置 `bin/Hostx64/x64`。

### 事实核对（供后续引用）
- ratatui 0.30 / crossterm 0.29 / eframe 0.36.1 / rfd 0.15 与文档一致；版本 0.1.3。
- `scripts/verify_gate.py`：CASES 10 个 + 11(pre-commit fail-closed) + 12/13(deny 包装) + 14(Unicode 转义，需二进制在 PATH)。
- CI：三平台 fmt/clippy/test/build；`gui` 特性仅在非 Linux 构建（Linux 缺 X11 库）→ UI 方案 P5 仍"部分"。
