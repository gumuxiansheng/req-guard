# 需求分析报告：req-guard（AI 需求门禁）

> 独立子工具，gates-toolkit 家族成员（与 sql-guard / java-guard / wan / bug-guard 同构）。
> 由 dev-scaffold 剥离而来（见 dev-scaffold `架构决策记录.md` ADR-001）。

---

## 1. 背景与定位

**问题**：AI 编码助手的常见失效模式是"跳过设计直接写代码"——需求没拆清、方案没评审、测试没规划。

**定位**：`req-guard` 是**流程门禁**——管"该不该写"；gates-toolkit 既有成员管"写得对不对"。

| 工具 | 管什么 | 时机 |
| --- | --- | --- |
| sql-guard / java-guard | 代码质量（SQL/Java 规范） | 提交时 |
| **req-guard** | **开发流程（AI 该不该写）** | **写代码前** |

**核心规则**：AI 在项目中实现新需求前，必须先按步骤完成
**需求分解 → 技术方案 → 测试计划** 三段清单，由审核人**逐段批准**；
存在未解决的**阻塞性评论**时同样拦截。未满足时对 AI 实施**硬拦截**（无法写入/修改文件）。

**为什么从 dev-scaffold 剥离**（ADR-001）：生命周期错配——脚手架是"创建时用一次"，
门禁是"整个开发周期常驻"。门禁归位 gates-toolkit 后可复用 setup-gates 安装体系、
适用任意存量项目、独立演进发版。

---

## 2. 功能范围

| 功能 | 说明 |
| --- | --- |
| 三段清单生命周期 | `create / approve / reject / status / list`；步骤状态机 `pending → approved/rejected` |
| 强制审核顺序 | 默认 `decomposition → solution → testplan`，前置未过不得审后一步 |
| 审核评论 | 审核人**只评论、不修改正文**；支持步骤级 + 行号锚定（`--quote` + `line` + 上下文） |
| 阻塞性评论 | `--blocking`：未 resolve 时即便三段 approved 也拦截 |
| AI 不可篡改 | 评论独立文件 + L1 hook 路径拦截 + 命令层约束（AI 只能 reply，**不能 resolve**） |
| 硬拦截 | L1 AI 工具 PreToolUse（最硬）→ L2 git pre-commit（fail-closed）→ L3 CI `check` |
| 应急绕过 | `bypass --reason --ttl`：有时效、必填原因、强制审计；**不覆盖评论证据保护** |
| 审计留痕 | 拦截/放行/绕过/评论事件全部写入 `audit/gate-audit.log` |
| 摘要曝光 | hook 每次触发输出评论摘要，保证 AI 必然看到审核意见 |

### 2.1 非功能需求

- **零外部依赖**：纯 Rust 标准库；拦截判定用 grep/sed（任意环境可用）。
- **非交互**：CI/脚本可直接调用。
- **中文提示**：结论先行 + 可操作。

### 2.2 角色权限（职责分离）

| 角色 | 能做 | 不能做 |
| --- | --- | --- |
| 审核人 | 加评论、批准/打回、**关闭（resolve）评论** | — |
| AI | 读评论、修改正文、**回复评论** | **resolve 评论**、直接写 `*.comments.md` |

---

## 3. 命令规格

```bash
req-guard init                        # 初始化 .gates/（脚本 + AI hook + pre-commit）
req-guard create   -t <标题> [--id REQ-001]
req-guard approve  <需求ID> --step <步骤> --reviewer <姓名> [--comment <意见>]
req-guard reject   <需求ID> --step <步骤> --reviewer <姓名> [--comment <意见>]
req-guard comment  <需求ID> --author <姓名> --text <意见> [--step] [--quote] [--blocking] [--reply C001]
req-guard resolve  <需求ID> <评论ID> --author <姓名>      # AI 禁止调用
req-guard status   [<需求ID>] | list
req-guard comments <需求ID> [--refresh-anchors]
req-guard check                        # 手动判定（退出码 0 放行 / 1 拦截），CI 可用
req-guard install   [--tool <a,b>]
req-guard bypass    --reason <原因> [--ttl 60]
```

通用：`-p/--path <项目根>`（默认当前目录）；身份回退环境变量 `REQ_GUARD_REVIEWER`。

---

## 4. 目录规范（`.gates/`）

> 命名说明：gates-toolkit 既有约定是 `gates-tools/`（渲染产物，**不入库**）。
> `.gates/` 是**新增约定**：随仓库提交的门禁资产。二者对称分工：

| 目录 | 内容 | 入库 |
| --- | --- | --- |
| `.gates/` | 需求清单、审核评论、拦截脚本、门禁声明 | **是**（clone 即生效） |
| `gates-tools/` | 二进制、渲染 hook、规则副本 | 否 |

```
<项目根>/
└── .gates/
    ├── req-guard.yaml              # 门禁声明（步骤、顺序、拦截点、绕过策略）
    ├── README.md                   # 门禁使用说明
    ├── requirements/
    │   ├── REQ-001-*.md            # 三段清单（人机双读，AI 可写正文）
    │   ├── REQ-001-*.comments.md   # 审核评论（AI 禁止直接写）
    │   └── .gitkeep
    ├── hooks/
    │   ├── req-guard-check.sh      # 拦截脚本（POSIX，唯一判定逻辑）
    │   └── req-guard-check.ps1     # Windows 等价实现
    └── audit/gate-audit.log        # 审计日志（.gitignore 忽略）
```

---

## 5. 与 gates-toolkit 的集成

1. **片段**：`templates/hooks/fragments/reqguard-check.sh`（本仓库提供），注册为
   `pre-commit` entry `030-reqguard (mode=block)`；
2. **注册**：`hookctl register --tool reqguard --hook pre-commit --priority 30 --mode block --role check`；
3. **装配**：`setup-gates` 调 `req-guard init`（dev-scaffold 的 `new` 透传 `--no-req-guard` 可关闭）；
4. **升级**：拦截脚本随仓库提交（clone 即生效）；规则/二进制走 toolkit 每日 update。

---

## 6. 验收口径

| 项 | 期望 |
| --- | --- |
| 清单生命周期 | create/approve/reject/status 全流程可用；强制顺序生效 |
| 硬拦截 | 无清单 / 未全批准 / 有未解决阻塞评论 → 退出码 1，AI 的 Write/Edit 被阻断 |
| 证据不可篡改 | AI 写 `*.comments.md` 被 L1 拒绝；`--author ai resolve` 被命令层拒绝 |
| 行号锚定 | `--quote` 定位行号；正文修改后 `--refresh-anchors` 可重算，未命中标记 stale |
| 绕过有痕 | bypass 生成有时效令牌；所有事件入审计日志 |
| 幂等安装 | 重复 install 不重复写入；已有 AI 工具配置不覆盖，提示手工合并 |
| 跨平台 | sh/ps1 双实现行为一致；`cargo check` 零警告 |
